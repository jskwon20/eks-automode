apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
spec:
  ingressClassName: nginx
  rules:
  - host: "gsitm-test.com"
    http:
      paths:
      - path: /app2
        pathType: Prefix
        backend:
          service:
            name: app2
            port:
              number: 80
      - path: /
        pathType: Prefix
        backend:
          service:
            name: app1
            port:
              number: 80